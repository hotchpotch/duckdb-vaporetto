# v0.3.0

- Fix DuckDB-Wasm `VARCHAR` input handling for short inline strings.
- Fix `vaporetto_split()` failures in Wasm builds for inputs such as
  `機械学習`, ASCII-only strings, and mixed Japanese/ASCII text.
- Keep the small and full DuckDB-Wasm release artifacts aligned with the native
  tokenizer behavior for short query strings.
